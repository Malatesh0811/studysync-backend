# StudySync backend — production dependencies
# Stack: FastAPI + SQLite (via SQLAlchemy) + local mock S3
# No boto3/psycopg2 needed — those are replaced by the mock S3 layer.

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.29
pydantic>=2.6.0
pydantic-settings>=2.2.1
python-multipart>=0.0.9
